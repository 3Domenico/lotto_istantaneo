export default function Bet(user_id, draw_id, bet_numbers, points_used) {
    this.user_id = user_id;
    this.draw_id = draw_id;
    this.bet_numbers = bet_numbers;
    this.points_used = points_used;
}